#include<stdio.h>
int main() {
    int num,limit;
    scanf("%d%d",&num,&limit);
    int sum=0;
    for (int i=1;i<=limit;i++) {
        if (i%num==0) {
            sum=sum+i;
        }
    }
    printf("%d",sum);

}
